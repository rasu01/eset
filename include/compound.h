#pragma once

namespace bunshi {

    //A collection of archetypes(molecules), where all the different types of entites are stored inside.
    //every archetype has different types of components put together
    class Compound {

    };
}